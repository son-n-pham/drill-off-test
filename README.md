Consideration to improve:

- Having rig, BHA and bit specifications input
- If mud motor is used, what mechanism for the user to have robust input of flow rate and surface RPM to calculate downhole RPM
- Bit RPM and WOB is controlled by WN number (WN = Bit RPM * WOB in klbf), maximum WOB allowed by rig and BHA specifications